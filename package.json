{
  "name": "livemrkd",
  "productName": "LiveMrkd",
  "version": "1.0.0",
  "description": "Simple markdown renderer",
  "main": "app/app.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager ./ LiveMrkd --platform=darwin --arch=x64 --out ./compiled --version=${ELECTRON_VERSION} --overwrite --icon ./app/icons/LiveMrkd-icon.icns"
  },
  "author": "Tyler Bunting",
  "license": "MIT",
  "devDependencies": {
    "electron-packager": "^7.0.0",
    "electron-prebuilt": "^0.37.6"
  },
  "dependencies": {
    "font-awesome": "^4.6.1",
    "jquery": "^3.5.0",
    "marked": "^0.3.5"
  }
}
